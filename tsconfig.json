{
  "compilerOptions": {
    "target": "es2020", // Node.jsのバージョンに合わせて調整
    "module": "commonjs", // Expressサーバーで require を使っているので commonjs
    "esModuleInterop": true, // import * as express from 'express' のようなインポートを許可
    "strict": true,          // 厳格な型チェックを有効化 (推奨)
    "skipLibCheck": true,    // 依存ライブラリの型チェックをスキップ (ビルド高速化)
    "forceConsistentCasingInFileNames": true,
    "outDir": "./dist",      // (オプション) ビルド時の出力先
    "rootDir": "./",         // (オプション) ソースファイルのルート
    "resolveJsonModule": true, // JSONファイルのインポートを許可
    "types": [
        "node", // Node.jsの型定義
        "vitest/globals" // VitestのグローバルAPIの型定義
    ]
  },
  "include": [
    "server.js", // または server.ts
    "tests/**/*.ts", // テストファイル
    "vitest.config.ts" // Vitest設定ファイル
    // プロジェクトに含まれる他の .ts ファイルや .js ファイル
  ],
  "exclude": [
    "node_modules",
    "dist" // ビルド出力先 (もしあれば)
  ]
}